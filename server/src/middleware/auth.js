const jwt=require('jsonwebtoken');const prisma=require('../lib/prisma');const asyncHandler=require('../utils/asyncHandler');const getCookie=(req,name)=>{const header=req.headers.cookie||'';const pair=header.split(';').map(p=>p.trim()).find(p=>p.startsWith(`${name}=`));return pair?decodeURIComponent(pair.slice(name.length+1)):null;};
const requireAdmin=asyncHandler(async(req,res,next)=>{const authHeader=req.headers.authorization||'';const bearer=authHeader.startsWith('Bearer ')?authHeader.slice(7):null;const cookieToken=getCookie(req,'rrp_admin_token');const token=bearer||cookieToken;if(!token)return res.status(401).json({message:'Authentication required.'});if(!process.env.JWT_SECRET||process.env.JWT_SECRET.length<32)return res.status(500).json({message:'Authentication is not configured securely.'});if(!bearer&&cookieToken&&!['GET','HEAD','OPTIONS'].includes(req.method)){const csrfCookie=getCookie(req,'rrp_csrf');const csrfHeader=req.headers['x-csrf-token'];if(!csrfCookie||!csrfHeader||csrfCookie!==csrfHeader)return res.status(403).json({message:'CSRF validation failed.'});}try{const payload=jwt.verify(token,process.env.JWT_SECRET);const admin=await prisma.admin.findUnique({where:{id:payload.id}});if(!admin)return res.status(401).json({message:'Admin account not found.'});req.admin={id:admin.id,name:admin.name,email:admin.email,role:admin.role};next();}catch{return res.status(401).json({message:'Invalid or expired token.'});}});module.exports={requireAdmin};
