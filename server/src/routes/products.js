const express = require('express');
const multer = require('multer');
const { z } = require('zod');
const prisma = require('../lib/prisma');
const asyncHandler = require('../utils/asyncHandler');
const { requireAdmin } = require('../middleware/auth');
const makeSlug = require('../utils/slug');
const { formatProduct } = require('../utils/money');
const { uploadBufferToCloudinary } = require('../utils/cloudinaryUpload');
const router = express.Router();
const upload = multer({ storage: multer.memoryStorage(), limits: { fileSize: 6 * 1024 * 1024 }, fileFilter(req,file,callback){ if(!['image/jpeg','image/png','image/webp','image/avif'].includes(file.mimetype)) return callback(new Error('Only JPEG, PNG, WebP, and AVIF images are allowed.')); callback(null,true); } });
const productSchema=z.object({name:z.string().trim().min(2).max(180),description:z.preprocess(v=>typeof v==='string'?v.trim():v,z.string().max(5000).optional().nullable()),price:z.coerce.number().positive(),costPrice:z.coerce.number().positive().optional().nullable(),salePrice:z.coerce.number().positive().optional().nullable(),size:z.string().trim().max(100).optional().nullable(),gender:z.string().trim().max(50).optional().nullable(),scentFamily:z.string().trim().max(100).optional().nullable(),occasion:z.string().trim().max(100).optional().nullable(),brandType:z.string().trim().max(100).optional().nullable(),notes:z.array(z.string().trim().max(100)).max(30).default([]),images:z.array(z.string().url()).max(20).default([]),stock:z.coerce.number().int().min(0).max(100000).default(0),isFeatured:z.boolean().default(false),isActive:z.boolean().default(true),categoryId:z.string().optional().nullable()}).superRefine((data,ctx)=>{if(data.salePrice!=null&&data.salePrice>data.price)ctx.addIssue({code:'custom',path:['salePrice'],message:'Sale price cannot exceed regular price.'});if(data.costPrice!=null&&data.costPrice>data.price)ctx.addIssue({code:'custom',path:['costPrice'],message:'Cost price cannot exceed regular price.'});});
const normalizeProductData=(data)=>({...data,description:data.description||'',costPrice:data.costPrice||null,salePrice:data.salePrice||null,categoryId:data.categoryId||null});
const uniqueSlug=async(baseSlug,excludeId=null)=>{for(let i=0;i<1000;i+=1){const candidate=i===0?baseSlug:`${baseSlug}-${i+1}`;const found=await prisma.product.findUnique({where:{slug:candidate},select:{id:true}});if(!found||found.id===excludeId)return candidate;}throw new Error('Could not generate a unique product slug.');};
router.get('/',asyncHandler(async(req,res)=>{const{search,category,featured,active,page,limit,availability,sort}=req.query;const pageNumber=Number.isFinite(Number(page))?Math.max(1,Number(page)):1;const pageSize=Math.min(48,Math.max(1,Number(limit)||24));const skip=(pageNumber-1)*pageSize;const where={isActive:active==='false'?undefined:true,isFeatured:featured==='true'?true:undefined,category:category?{slug:String(category)}:undefined,stock:availability==='available'?{gt:0}:undefined,OR:search?[{name:{contains:String(search),mode:'insensitive'}},{description:{contains:String(search),mode:'insensitive'}}]:undefined};const orderBy=sort==='low'?{price:'asc'}:sort==='high'?{price:'desc'}:{createdAt:'desc'};const[products,total]=await Promise.all([prisma.product.findMany({where,skip,take:pageSize,include:{category:true},orderBy}),prisma.product.count({where})]);res.json({products:products.map(formatProduct),pagination:{page:pageNumber,limit:pageSize,total,totalPages:Math.ceil(total/pageSize),hasMore:pageNumber*pageSize<total}});}));
router.get('/admin/all',requireAdmin,asyncHandler(async(req,res)=>{const products=await prisma.product.findMany({include:{category:true},orderBy:{createdAt:'desc'}});res.json({products:products.map(formatProduct)});}));
router.post('/upload-image',requireAdmin,upload.single('image'),asyncHandler(async(req,res)=>{if(!req.file)return res.status(400).json({message:'Please choose a product image.'});const result=await uploadBufferToCloudinary(req.file,'roc-realm-products');res.status(201).json({imageUrl:result.secure_url,publicId:result.public_id});}));
router.get('/:slug',asyncHandler(async(req,res)=>{const product=await prisma.product.findUnique({where:{slug:req.params.slug},include:{category:true}});if(!product||!product.isActive)return res.status(404).json({message:'Product not found.'});res.json({product:formatProduct(product)});}));
router.post('/',requireAdmin,asyncHandler(async(req,res)=>{const data=productSchema.parse(req.body);const slug=await uniqueSlug(makeSlug(data.name));const product=await prisma.product.create({data:{...normalizeProductData(data),slug},include:{category:true}});res.status(201).json({product:formatProduct(product)});}));
router.put('/:id',requireAdmin,asyncHandler(async(req,res)=>{const data=productSchema.parse(req.body);const current=await prisma.product.findUnique({where:{id:req.params.id}});if(!current)return res.status(404).json({message:'Product not found.'});const nextSlug=current.name===data.name?current.slug:await uniqueSlug(makeSlug(data.name),req.params.id);const product=await prisma.product.update({where:{id:req.params.id},data:{...normalizeProductData(data),slug:nextSlug},include:{category:true}});res.json({product:formatProduct(product)});}));
router.delete('/:id',requireAdmin,asyncHandler(async(req,res)=>{await prisma.product.delete({where:{id:req.params.id}});res.json({message:'Product deleted.'});}));
module.exports=router;
